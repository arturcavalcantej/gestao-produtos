version: "3"

services:
  db:
    image: postgres
  dj:
    container_name: dj
    build: .
    command: python manage.py runserver 0.0.0.0:80
    volumes:
      - .:/code
    ports:
      - "80:80"
    depends_on:
      - db